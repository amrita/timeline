alert('ajax works updating event');
//alert('do we have any elements left in the events object ' + eventObjects.length);
$("#last-saved").text("Your isles were last saved at <%= Time.now.strftime("%Y-%m-%d %H:%M:%S") %>");